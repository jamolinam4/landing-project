````

Steps to deploy:

1. Initialize Git repository (if not already done):
```bash
git init
```

2. Create a new repository on GitHub named "landing-page-project"

3. Add your remote repository:
```bash
git remote add origin https://github.com/juliethmolina/landing-page-project.git
```

4. Create and switch to main branch:
```bash
git checkout -b main
```

5. Add, commit and push your changes:
```bash
git add .
git commit -m "Initial commit"
git push -u origin main
```

6. In your GitHub repository settings:
   - Go to "Settings" > "Pages"
   - Set source branch to "gh-pages"
   - Set folder to "/ (root)"
   - Save the changes

Your site will be available at: `https://juliethmolina.github.io/landing-page-project`

Note: Make sure to:
- Replace "juliethmolina" with your actual GitHub username
- Update the paths in `_config.yml` accordingly
- Enable GitHub Pages in your repository settings
- Add any necessary environment variables in your repository secrets